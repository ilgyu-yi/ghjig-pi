# SPEC — the behavioural single source of truth

This document is the repository's behavioural SSOT: every enforced norm, gate class, and workflow contract is homed here. `MISSION.md` states direction; this SPEC states behavior — MISSION content is cited, never restated.

## Table of contents

<!-- TOC START — generated by .github/workflows/build_toc.sh; do not edit by hand -->
| Section | Title | Line |
|---|---|---|
| §0 | Intent and scope | 42 |
| &nbsp;&nbsp;§0.1 | What the SPEC governs | 46 |
| &nbsp;&nbsp;§0.2 | Relation to MISSION | 50 |
| &nbsp;&nbsp;§0.3 | Reading and amendment conventions | 54 |
| §1 | Work norms | 58 |
| &nbsp;&nbsp;§1.1 | The standard flow | 62 |
| &nbsp;&nbsp;§1.2 | Doc → Test → Code work order | 73 |
| &nbsp;&nbsp;§1.3 | Changelog-fragment discipline | 83 |
| §2 | Artifact hierarchy and lifecycle | 87 |
| &nbsp;&nbsp;§2.1 | Issue types | 91 |
| &nbsp;&nbsp;§2.2 | Lifecycle states | 102 |
| &nbsp;&nbsp;§2.3 | PR-as-living-doc | 112 |
| &nbsp;&nbsp;§2.4 | Evidence discipline | 116 |
| §3 | Enforcement-layer architecture | 126 |
| &nbsp;&nbsp;§3.1 | The constraint | 130 |
| &nbsp;&nbsp;§3.2 | The three tiers | 137 |
| &nbsp;&nbsp;§3.3 | Gate classes | 145 |
| &nbsp;&nbsp;§3.4 | Agent-agnosticism of the tiers | 163 |
| &nbsp;&nbsp;§3.5 | Gate conduct | 167 |
| §4 | Substrate and install contract | 171 |
| &nbsp;&nbsp;§4.1 | Namespaces | 175 |
| &nbsp;&nbsp;§4.2 | Target-parameterization | 179 |
| &nbsp;&nbsp;§4.3 | PR-based installs | 183 |
| &nbsp;&nbsp;§4.4 | Headless and scripted operation | 187 |
| §5 | Cross-cutting contracts | 191 |
| &nbsp;&nbsp;§5.1 | Self-contained artifacts | 195 |
| &nbsp;&nbsp;§5.2 | Graceful degradation | 199 |
| &nbsp;&nbsp;§5.3 | Gate-activation conditions | 203 |
| &nbsp;&nbsp;§5.4 | Work language | 207 |
| §6 | Self-governance milestone | 211 |
<!-- TOC END -->

## 0. Intent and scope

This section states what the SPEC governs, its subordination to `MISSION.md`, and the reading conventions — the marker-delimited table of contents exists so readers take targeted section reads instead of loading the whole document.

### 0.1 What the SPEC governs

The SPEC governs the operating shell this repository ships — the norms it enforces on work performed in repositories that adopt it — and, identically, this repository's own development. There is one rulebook: a norm that binds an adopter binds this repository first.

### 0.2 Relation to MISSION

`MISSION.md` is the direction document; this SPEC is the behavior document. The SPEC derives its requirements from the MISSION's success observables and NOT-goals, cites them by their keyed names (Traceability, Enforcement, Agent-agnosticism, Evidence — `MISSION.md` § "Success looks like"), and never restates their content normatively. A contradiction between the two is a SPEC defect: MISSION wins on direction, and the SPEC must be amended through the standard flow (§1.1).

### 0.3 Reading and amendment conventions

Sections are numbered (`## N.` / `### N.M`) and indexed by the generated table of contents, which records line numbers for targeted reads. Every edit to this document regenerates the table of contents in the same commit (`.github/workflows/build_toc.sh`; freshness is CI-enforced by `.github/workflows/check-toc.yml`). Amendments ride the standard flow like any other change, doc phase first (§1.2). Supporting documents, if a `docs/` tree ever exists, are thin pointers that lead with a reference into this SPEC (`.github/workflows/check-ssot-home.sh` enforces the discipline); contract prose lives here, not there.

## 1. Work norms

This section states the standard flow every change rides — issue → branch → draft PR → review-gated merge — and the Doc → Test → Code work order with its strict and relaxed conditions, including the per-PR changelog-fragment discipline.

### 1.1 The standard flow

Every change to a governed repository rides one backbone: **issue → branch → draft PR → checklist commits → ready PR → review-gated merge**. No change reaches the default branch outside it. The elements:

- **Issue first.** Work starts from an open, Active issue (§2.2) that states what is broken or needed, why it serves the mission, and verifiable acceptance criteria.
- **Branch naming.** `<owner-username>/<type>/[<issue#>-]<slug>` — e.g. `ilgyu-yi/docs/5-author-spec`.
- **Commit convention.** `<type>(#<N>)[!]: <subject>`, subject 1–72 codepoints. The required group (`feat` / `fix` / `docs` / `refactor` / `perf`) must carry an issue number; the optional group (`test` / `style` / `build` / `ci` / `chore` / `revert`) may carry one (an issue or PR number). The grammar is enforced at the local tier (§3.2).
- **Draft PR early.** The PR opens as a draft with its first real commit — never an empty seed commit — and its body is maintained as a living document (§2.3).
- **Linkage.** The PR body's first line carries `Closes #N` when merging to the default branch closes the issue, `Refs #N` for intermediate work.
- **Merge shape.** Merges to the default branch use a merge commit (no fast-forward, no squash), so the branch's phase commits (§1.2) stay readable on the trunk. The server-side ruleset (§3.3) enforces both faces: the merge-commit method is the only one it allows into the default branch, and history is non-fast-forward. Squash and rebase remain legitimate on non-default bases (topic-branch consolidation), which the ruleset does not govern.

### 1.2 Doc → Test → Code work order

Within one issue, work proceeds in three phases, each its own commit, in order:

1. **Doc** — write the contract into the SSOT first (this SPEC, `MISSION.md`, `README.md`, or an ADR for irreversible decisions). The contract exists before anything asserts it.
2. **Test** — translate the doc into a failing check and observe the intended failure. A check that has never failed proves nothing; the failing observation is pinned as evidence (§2.4).
3. **Code** — the minimal change that turns the failing check green.

**Strict** for: new capabilities (`feat`), documentation contracts (`docs`), and any change to an external contract, API, or schema. **Relaxed**, with the reason stated in the PR body's Plan, for: `fix` (reproduce first — the reproduction is the failing test), `refactor` (doc phase skippable when external behavior is unchanged), `perf` (measure first), spikes (no phases, no branch, no PR — the change surface is empty and the terminal artifact is a findings note on the issue, e.g. issue #4), and trivial corrections. For a pure-document deliverable the phases re-map rather than disappear: Doc is the document's architecture, Test is the pre-existing gates exercised failing-first, Code is the content.

### 1.3 Changelog-fragment discipline

Every PR to the default branch lands exactly one changelog fragment `changelog_unreleased/<category>/<N>.md` — `<category>` one of the six Keep-a-Changelog categories (`changelog_unreleased/TEMPLATE.md`), `<N>` the PR number or a closing issue number — or carries the `skip-changelog` label for changes with no user-observable effect. A fragment describes only what its PR lands, in one bullet. CI enforces the floor — at least one valid in-allow-set fragment or the skip label, with every added fragment valid (`.github/workflows/check-changelog.yml`, the `fragment-gate` required check); the exactly-one norm above that floor is review-enforced (§2.3). Fragments are consolidated into a versioned changelog at release time; the fragment tree empties with each release.

## 2. Artifact hierarchy and lifecycle

This section states the issue types and their lifecycle states (proposed → active → closed), the PR-as-living-doc contract, and the evidence discipline durable artifacts must satisfy.

### 2.1 Issue types

The type of an issue is its label — never inference from prose. The shipped issue-form templates (`.github/ISSUE_TEMPLATE/`) file each locally-filed type with its label preapplied; `initiative` alone has no template, because an Initiative arrives from upstream and is never filed here:

- **`task`** — a standalone unit of work.
- **`bug`** — a defect against documented behavior.
- **`execution`** — a unit of work parented under a Directive (its body's first line carries `Parent Directive: #N`).
- **`directive`** — a scoping tier above execution: a Directive states an outcome, success signals, non-goals, and constraints; Execution issues do the work under it. Directives do not branch into engineering PRs themselves.
- **`discussion`** — a friction-free tier for observations and half-formed ideas; closed either promoted (a concrete issue filed) or no-action.
- **`initiative`** — a planning tier above Directives, consumed from upstream: read-only to the shell except for structured comments.

### 2.2 Lifecycle states

States are encoded as labels; issues are the SSOT and any project-board mirror is derived, never authoritative.

- **Proposed** (`status:proposed`, auto-stamped at filing) — filed but not yet actionable. No branch is created against a Proposed issue.
- **Active** (no status label) — the Proposed → Active flip requires **activation evidence**: a substance review of the current body whose pass verdict is posted as an issue comment with the marker `<!-- activation-verdict: pass -->` as its first line, by a trusted account (owner/member/collaborator), before the `status:proposed` label is removed. Two review moments are complementary, not redundant: an author-side check before filing, and this observer-side check at activation.
- **`awaiting-author`** — a reviewer returned a non-pass verdict; the author owes the next move.
- **`status:blocked`** — a Directive that cannot proceed without external input; unblocking re-validates the current body through the same activation gate.
- **Closed** — `completed` only through the flow (merge closes via `Closes #N`, or an explicit completion review for Directives); `not planned` otherwise. Before a merge that closes an issue, each of the issue's acceptance criteria is resolved in an AC-closeout comment: checked, or marked N/A with a one-line reason.

### 2.3 PR-as-living-doc

The PR body is editorial, not append-only. After each commit the body is curated to remain true: stale plan items are removed or updated, and the order is always **commit → body update** — the fact lands first, then the reflection. Before any automated body update the remote body is refetched; if it changed externally, the update aborts rather than clobbering. At merge time every checklist item in the body is terminal: `[x]` done, `[~] N/A — <reason>`, or removed. A draft PR becomes ready only when its gates hold: review passed at the current head (§3.3), CI green, body curated, changelog discipline satisfied (§1.3).

### 2.4 Evidence discipline

Durable artifacts — issue bodies, PR bodies, commit messages, review verdicts — carry their claims as evidence, never as unverifiable assertion (MISSION § "Success looks like > Evidence"):

- A quotation resolves at the file it is attributed to.
- A claim about a corpus ("zero matches", "all files pass") carries the command that measured it and its output, pinned to a commit.
- No claim that expires before the artifact does — expiring claims are removed or pinned to their snapshot, never left to rot.
- A self-claim about the same change ("localized", "reviewed") ships its evidence or is not written.
- Reviews are pinned to the exact commit they examined (`reviewed-head`); a verdict for one head never carries forward to another.

## 3. Enforcement-layer architecture

This section states the first-class design constraint — pi provides no built-in permission system, so the shell's enforcement layer is the only safety net — and enumerates the gate classes across three tiers: in-process pi-extension gates, the local git-hook tier, and CI gates.

### 3.1 The constraint

**pi provides no built-in permission system.** Nothing in the substrate asks for approval before a tool call executes; nothing else stands between an agent's mistake and the repository. The shell therefore treats its enforcement layer as the only safety net — a commitment `MISSION.md` states as substrate-evolution-proof: it holds regardless of any permissions the substrate may later grow. Two consequences bind every design in this section:

1. Every norm this SPEC declares is either **enforced** — a gate in at least one tier blocks or surfaces its violation — or **explicitly advisory**, and says which.
2. The enforcement layer's own failure modes are part of its contract: a gate that can silently not-fire is a defect (§5.2, §5.3).

### 3.2 The three tiers

**Tier 1 — in-process substrate gates.** pi extensions, loaded from the repository's extension directory, subscribe to the substrate's tool-call events and can **block** a call (returning a reason the agent observes) or **mutate** its input before execution; they can register commands, receive session-lifecycle events, and persist state across a session. All five behaviors were probed and verified against the pinned substrate environment — the spike note on issue #4 is the evidence record. Constraints observed there bind tier-1 implementations: extension factories must not invoke action methods at load time (register work in session-start handlers instead); headless invocation requires an explicit end-of-input on stdin and an explicit project-trust flag; a scripted provider requires a placeholder endpoint value; and the scripted-provider pattern is the designated **hermetic test substrate** — tier-1 gates are testable without network or model access. This tier is in-session mistake prevention: it narrows the loop for agent-driven work but is not a security boundary.

**Tier 2 — the local git-hook tier.** The committed `.githooks/` adapters (`pre-commit`, `pre-push`, `commit-msg`) bind any local git operation — human, script, or any agent harness — once a clone activates them (`core.hooksPath=.githooks`). This is an **advice tier**: it folds to `--no-verify` by design, and a clone with no shell binding no-ops rather than wedging git. The adapters carry no check logic; they delegate through the contract declared in `.githooks/_lib.sh`: a per-clone, untracked adapter (`.ghjig/shell-adapter.sh`) provides the runtime (`safe_source`, `audit_log`) and a helper directory supplying the delegated checks (branch guarding, staged-secret scanning, commit-subject grammar). A present-but-incomplete helper degrades to allow, never to a false block.

**Tier 3 — CI gates and the server-side ruleset.** The workflow gates (`fragment-gate`, `ssot-home`, `toc-freshness`) run as required status checks, and the branch ruleset adds non-fast-forward history, deletion protection, and review-thread resolution. This tier is the **hard floor**: it binds every contributor and every clone, including one where tiers 1–2 are absent or bypassed.

### 3.3 Gate classes

The gate classes the enforcement layer commits to, with their current tier homes:

| Class | Guards against | Tier home (current) |
|---|---|---|
| protected-branch | direct commit/push to the default or release branches | tier 2 (adapters); tier 3 (ruleset — default branch only today); tier 1 planned first |
| force-push | history rewrites on protected refs | tier 2 (by subsumption); tier 3 (non-fast-forward rule) |
| secret | committing a staged secret | tier 2 (delegated scan) |
| commit-format | malformed commit subjects | tier 2 (delegated grammar check) |
| changelog | a PR landing with no fragment and no skip label | tier 3 (`fragment-gate`) |
| ssot-home | contract prose drifting out of the SPEC | tier 3 (`ssot-home`) |
| toc-freshness | a stale SPEC table of contents | tier 3 (`toc-freshness`) |
| merge-review | merging without a review pinned at the merged head | procedural today (§2.3); tier 1 planned |
| ac-closeout | merging a PR whose closing issue has unresolved AC | procedural today (§2.2); tier 1 planned |

"Planned" rows are commitments, not implementations: each tier-1 gate lands through its own Doc → Test → Code cycle, first the protected-branch class as the walking skeleton. A class with no live gate in any tier is advisory and must be labeled so wherever it is stated.

### 3.4 Agent-agnosticism of the tiers

No enforced norm depends on a specific agent or model (MISSION § "Success looks like > Agent-agnosticism"). Tier 2 binds at the git layer and tier 3 at the platform layer — both are indifferent to who or what performed the work. Tier 1 is substrate-specific by nature (it rides pi's extension API), but it narrows the loop rather than defining the floor: a different agent harness operating in the same clone is still bound by tiers 2–3 in full.

### 3.5 Gate conduct

Every blocking message names its positive remediation — the command or path that satisfies the norm — never a bare refusal (observed shape throughout the shipped gates, e.g. `.githooks/pre-push` and `.github/workflows/check-toc.yml`). Every block, and every sanctioned bypass, is observable: recorded to the audit surface available at that tier (the platform's comment/check surface for tier 3; the bound runtime's audit log for tier 2 today). A sanctioned escape is scoped, explicit, and logged — a silent bypass channel is a defect. Concrete tier-1 escape mechanics are specified with the tier-1 implementation cycle.

## 4. Substrate and install contract

This section states how shell assets install into a repository: target-parameterized, homed in shell-owned namespaces that collide with no other tooling's, and landed through PR-based installs.

### 4.1 Namespaces

The shell owns exactly two working-tree namespaces in a governed repository: `.pi/` (substrate-native runtime assets — extensions, when tier 1 lands) and `.ghjig/` (per-clone untracked state — the shell binding, never committed). Repository-visible substrate lives in standard platform locations: `.github/` (templates, workflows), `.githooks/` (the local tier), `changelog_unreleased/` (the fragment tree). The shell installs nothing into a namespace owned by other tooling, and enforced behavior never depends on such a namespace existing.

### 4.2 Target-parameterization

Committed substrate carries no absolute paths, no clone-specific values, and no knowledge of any particular development environment's layout. Anything clone-specific — above all the binding to whatever runtime backs the local tier — is generated per-clone as untracked state under `.ghjig/` (§3.2). The same committed bytes must work in every clone of every adopting repository.

### 4.3 PR-based installs

Substrate lands in an adopting repository through the standard flow — a reviewed PR (§1.1) — never a direct push. The one exception is the stage-0 seed of an empty repository (an unborn default branch cannot host a PR); that seed is minimal (the direction documents), scoped, and audit-logged. Server-side configuration (labels, the branch ruleset, board mirrors) is applied via the platform API; the repository records the shape server config must match — above all that required-check contexts equal the CI job names (`fragment-gate`, `ssot-home`, `toc-freshness`) and that the default branch's allowed merge method is the merge commit alone (§1.1).

### 4.4 Headless and scripted operation

Any scripted install, verify, or test step that drives the substrate honors the constraints recorded in the issue #4 spike note: headless invocations receive an explicit end-of-input on stdin and pass the explicit project-trust flag, and scripted-provider configurations supply the required placeholder endpoint value. Install tooling that ignores these hangs or fails spuriously; the spike note is the pinned reference for the observed behaviors.

## 5. Cross-cutting contracts

This section states the contracts that bind every section: self-contained artifacts, graceful degradation for best-effort automations, and explicit activation conditions for gates that sleep until their subject exists.

### 5.1 Self-contained artifacts

Every durable artifact in a governed repository — this SPEC included — states its rationale from first principles and cites only artifacts that resolve in-repo: files, issues, PRs, and commits of this repository. No external project, foreign specification, or agent-specific tooling is normative, and "another project does it this way" is never a rationale. The one legitimate external subject is the pi substrate itself, grounded through `MISSION.md`. Names of specific agents, models, or vendors do not appear in committed bytes; interfaces owned by an external runtime are confined to per-clone untracked state (§4.2). A mechanical token sweep as a CI gate class is a candidate refinement; until it exists, the norm is enforced at review (§2.3, §2.4).

### 5.2 Graceful degradation

A best-effort automation fails **open with a surfaced signal**, never closed and never silently: a missing optional dependency, an incomplete helper, or an unreachable service degrades the automation to a no-op or a warning — it does not wedge git, block the flow, or misattribute a cause. The recorded shape: the local tier's advisory-face guard (§3.2) turns a present-but-incomplete helper into a no-op instead of a false block. Degradation of an **enforcing** gate is different: a gate that fails must fail *closed* on its own evidence check (a missing pass comment blocks the flip; an unresolvable review blocks the merge) — fail-open is for best-effort aids, fail-closed for evidence gates, and every gate's contract says which it is. Where a degraded state is silent today (an unregistered clone's local tier no-ops with no notice), the silence is an accepted implementation choice, tightenable to a notice without SPEC change; the fail-open direction itself is definitional.

### 5.3 Gate-activation conditions

A gate whose subject does not yet exist **sleeps clean and says so**: its skip condition is part of its contract, stated where the gate is implemented (observed: `check-toc.yml` and `check-ssot-home.sh` skip while no `SPEC.md` exists, and each names that condition in its own text). Two obligations follow: the skip must be a deliberate branch, not an accidental fall-through; and a sleeping gate's **first activation is probed at the smallest possible change** — the change that creates the subject is pushed and verified against the real CI environment before anything larger rides on the gate (this SPEC's own PR exercised exactly that probe — issue #5).

### 5.4 Work language

Durable artifacts are authored in the repository's configured work language (default: English); the conversational language of any particular working session is a separate channel and never leaks into committed artifacts. One artifact, one language — mixed-language durable artifacts are a defect.

## 6. Self-governance milestone

This section states the criterion for the point at which this repository's own development runs under the shell it ships.

The milestone is reached when both hold:

1. **Tier 1 self-applied** — the in-process gate for the protected-branch class (§3.3) loads from this repository's own tree in the sessions that develop this repository, blocking the norm violations it specifies here.
2. **Tier 2 self-bound** — the per-clone adapter (§3.2) in this repository's development clones binds to the runtime this repository ships, not to any external one.

Until then, an external development environment may govern the working process, under one absolute condition: it never surfaces in committed artifacts (§5.1). After the milestone, this repository is its own first adopter, and every norm in this SPEC is exercised on the shell's own development before any adopter meets it.
