- `fragment-gate` no longer fails a draft pull request for a missing changelog fragment: it now sleeps clean while both the event payload and the live pull request state report a draft, reports the sleep and the `gh pr ready` recovery in the run's warning and job summary, wakes on the ready-for-review transition, and cancels a superseded run of the same pull request so a draft-era result cannot land after a ready-era one at the same head. (#45)
