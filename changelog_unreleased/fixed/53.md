- The two recovery clauses composed as commands to paste (the sink verdict's `chmod 600` and remove acts) now render their path POSIX single-quoted, so a command-substitution-shaped path component no longer executes when the clause is pasted; warning-surface escaping now also covers U+061C (ARABIC LETTER MARK), and the escaping helper's header discloses the non-bidi invisible-format residual (the U+200B class) in place. (#53)
