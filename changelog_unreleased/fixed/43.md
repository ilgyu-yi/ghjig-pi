- `fragment-gate` now reads the pull request's net file list with per-file status instead of the union of its per-commit patches, so a correction to an already-merged changelog fragment no longer fails the check, and a fragment touched and then reverted in the same branch no longer blocks it. (#43)
