- Warnings and refusals now escape every interpolated path through one `quoted()` helper, so a hostile path component can no longer forge a line into the degradation banner or land ANSI control bytes on the terminal; a structural test locks every new interpolation to the same rule. (#47)
