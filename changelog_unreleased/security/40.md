- An evidence sink the runtime creates is readable only by the account that writes it — the mode is carried on the create, so a sink an earlier runtime already created keeps whatever bits it was created under — the sink's destination cannot be redirected by a symlink at its path; a degradation of the append names a recovery live for the shape that failed; repository-root discovery no longer escapes its own install-root bound or consults the process working directory (#40).
