- The evidence sink is created readable only by the account that writes it, and its destination cannot be redirected by a symlink at its path; repository-root discovery no longer escapes its own install-root bound or consults the process working directory (#40).
