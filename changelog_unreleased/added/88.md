- The tier-1 runtime now holds the delegation layer: a `gitjig_dispatch` tool (`.pi/extensions/gitjig/dispatch/`) that resolves the expected head exactly once in the caller repository, provisions a detached no-hardlinks clone in a disposable scratch as the delegate's local writable world — the routes the clone plants back to the caller repository are severed at provision, though the delegate runs in the caller's trust domain and inherits its credentials — runs the delegate as a bounded argv child with the repo-locating and config-injection `GIT_*` families removed and the one state seam rebound into the scratch, and admits nothing but the ≤64 KiB closed-schema `return.json` — every §3.10 outcome class refused on a fixed content-free cause, the head compare crossing back as validity alone, a held-operand hex run in the summary refusing the return whole, and each act landing a `category:"dispatch"` audit record. (#88)
