- The local git-hook tier now scans the staged diff for secrets at commit time and arms the protected-branch commit check: `.githooks/helpers/secret_scan.sh` matches the added lines of each staged path against the committed pattern set in `.githooks/helpers/secret-patterns` (pattern IDs: private-key, aws-access-key-id, github-token, bearer-token), refuses with content-free records naming the pattern ID and sanitized path, honors a repo-root `.shellsecretignore` allow-list as domain exclusion, refuses unmeasurable (binary) staged inputs on their own cause, and disarms with one plain "not enforced" audit warn record when its own machinery degrades. (#66)
