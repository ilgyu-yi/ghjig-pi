- The local git-hook tier now enforces the protected-branch push gate: `.githooks/helpers/branch_guard.sh` derives the one protected identity from the clone's own remote pointer (with an `ls-remote` fallback on the push surface), refuses pushes byte-equal or ASCII-case-fold-equal to it with content-free causes, and disarms with one plain "not enforced" audit warn record when the identity is underivable. (#59)
