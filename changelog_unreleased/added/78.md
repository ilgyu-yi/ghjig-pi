- Added the command layer to the SPEC (§4.8): what a command asset is, a four-rung rule that returns exactly one registration surface for a required capability, the governed-home conjunct that keeps a selected surface inside `.pi/` by rule rather than by a path list, the canonical-name rule and the measured collision behaviour behind it, and what a repository is left with where the layer is absent. (#78)
