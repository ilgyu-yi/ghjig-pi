- The tier-1 runtime now holds the egress publish boundary: a `gitjig_publish` tool (`.pi/extensions/gitjig/publish/`) that scans every body against the same committed `.githooks/helpers/secret-patterns` file the commit-time gate reads — NUL-bearing bodies refused out-of-domain, format characters stripped before per-line byte-domain matching, refusals recorded content-free with pattern IDs and line locators — neutralizes relayed mentions and actionable references to backtick-wrapped inert spellings (the bare same-repo `#N` stays live), and publishes through a bounded `gh` child admitted on comment-URL output validity alone, with `outcome-unverified` reported on post-send ambiguity and no child-stream bytes in any record. (#83)
