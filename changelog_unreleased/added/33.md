- Land the first in-process gate that actually blocks: a commit or push targeting the default branch is refused in-session with a message naming the topic-branch → PR path and an audit record on every block, while topic-branch work passes untouched; the block has no in-session escape — the door for this tier is the session boundary, where the git and platform tiers observe the act and hold the record — and a registered `/ghjig-topic` command creates and switches to a correctly-named topic branch so the blocked next step is one command. (#33)
