- The local git-hook tier now checks commit subjects at commit time: `.githooks/helpers/conventional_commit.sh` ships the `<type>(#<N>)[!]: <subject>` grammar with 1..72-codepoint bounds, refuses unmeasurable subjects with a content-free cause, and leaves `--no-verify` as the tier's escape. (#55)
