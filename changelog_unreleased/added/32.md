- Land the tier-1 runtime foundation: a pi extension scaffold under `.pi/extensions/` (session-start registration, no action methods at load) with self-location, seam-aware state-root resolution, a one-line audit-record primitive, and the §3.9 fail-posture inventory — plus the hermetic scripted-provider test harness and the unit/integration suites covering issue #32's acceptance criteria. Shell-created state under `.ghjig/` is excluded from version control at creation. (#32)
