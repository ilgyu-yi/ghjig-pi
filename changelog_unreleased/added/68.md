- The tier-2 local git-hook tier derives its own locations: `.githooks/_lib.sh` carries the tier's runtime as committed code and resolves the helper directory from its own installed position and the record sink from the top of the repository those adapters are committed in, refusing to run any check where that repository is not the one the operation runs against. Arming a clone is one idempotent run of `bash .githooks/bind_local_tier.sh` from the repository root, which activates `core.hooksPath`, excludes the untracked state directory from version control at creation, and verifies the effective bound state; the session surface's bind advisory names a degraded state and that exact re-arm command, classifying from the configuration git resolves and running no program of the repository it classifies. (#68)
