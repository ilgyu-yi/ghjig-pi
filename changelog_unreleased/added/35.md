- `README.md`'s "Getting started" now states the suite invocation contract: the canonical `node --test "test/*.test.ts"` form with the glob quoted, the false-red a bare `node --test` produces by executing harness assets, and the two runtime prerequisites (`pi` on `PATH`; a Node.js runtime with native TypeScript type-stripping). (#35)
