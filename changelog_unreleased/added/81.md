- Added the delegation layer to the SPEC (§4.9): what a dispatch instrument is, the capability-keyed homing rule with its one-home many-call-sites shape, the content-free-return prohibition grounded on a measured transcript re-entry, the caller acts mapped as call sites of the §3.3-row predicates they produce evidence for, and what a repository is left with where the layer is absent. (#81)
